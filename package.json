{
  "name": "book-library",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "start-node": "node ./app.js",
    "build-production": "npm install --prefix ./frontend/book-library && npm run --prefix ./frontend/book-library build-prod",
    "start": "npm run build-production && npm install && npm run start-node"
  },
  "keywords": [],
  "author": "DD",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "concurrently": "^5.3.0",
    "http-server": "^0.12.3"
  }
}
